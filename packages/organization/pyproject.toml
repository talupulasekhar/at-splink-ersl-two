[project]
name = "organization"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sekhar talupula", email = "sekher.talupula@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
